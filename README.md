# Embedded-JSONRPC-Stability-Tester
Serial JSON-RPC stability and state validation tool for embedded devices over UART. Automates get_device_info, set_variant, and repeated set_state transitions (alert, syserr, mode) with configurable frequency and duration. Generates structured TSV logs for latency and communication analysis.
